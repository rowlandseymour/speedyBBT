# Construct the Bradley–Terry design matrix

Construct the Bradley–Terry design matrix

## Usage

``` r
construct.design.matrix(n.objects)
```

## Arguments

- n.objects:

  the number of objects

## Value

design matrix X

## Examples

``` r

# design matrix with 3 objects

X <- construct.design.matrix(3)
#> Error in construct.design.matrix(3): could not find function "construct.design.matrix"
```
