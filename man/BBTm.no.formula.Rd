% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{BBTm.no.formula}
\alias{BBTm.no.formula}
\title{Bayesian Bradley--Terry model with comparison-specific effect}
\usage{
BBTm.no.formula(
  outcome,
  item1,
  item2,
  item.prior.var,
  lambda.initial,
  advantage = NULL,
  kappa.initial = NULL,
  kappa.var = NULL,
  n.iter = 1000,
  hyperparameter = TRUE,
  chi = 0.01,
  psi = 0.01,
  burn.in = 100,
  n.thin = 1,
  verbose = interactive()
)
}
\arguments{
\item{outcome}{Vector of outcomes. 1 if item2 is the winner,
0 if item1 is the winner.}

\item{item1}{Vector of first items.}

\item{item2}{Vector of second items.}

\item{item.prior.var}{(optional) Matrix specifying the prior covariance of
the item correlation parameters.}

\item{lambda.initial}{(optional) Vector containing the values of the
item parameters for the first MCMC iteration.}

\item{advantage}{(optional) A vector with the value of the comparisons specific
effect for each comparison.}

\item{kappa.initial}{(optional) An initial value for the comparison specific
value kappa.
@param kappa.var (optional) The prior variance of the comparison specific
value kappa.}

\item{n.iter}{Number of MCMC samples to be drawn.}

\item{hyperparameter}{Boolean indicating if inference should be performed
for the prior variance hyperparameter. If TRUE the prior variance
(main diagonal of the covariance matrix) must be set to 1.}

\item{chi}{Rate parameter for the inverse-gamma prior distribution on the
hyperparameter.}

\item{psi}{Shape parameter for the inverse-gamma prior distribution on the
hyperparameter.}

\item{burn.in}{Number of iterations to use as a burn-in period. Default is 100.}

\item{n.thin}{(optional) The number of iterations to thin the MCMC samples by. Default is 1.}

\item{verbose}{(optional) A boolean indicating if progress should be printed to the console.
Default is \code{interactive()} therefore progress is shown if run interactively, but disabled in non-interactive contexts.}
}
\value{
A \code{coda::mcmc} object containing samples from the posterior distribution
}
\description{
This function fits the Bradley-Terry model with a comparison specific effect.
Each comparison can be assigned a real value to allow for a specific effect
for the comparison, such as bias, ordering or home/away effect. The value of
this effect is denoted $kappa$. The function places a normal prior distribution
on both kappa and the item quality parameters lambda.
}
\details{
If \code{item.prior.var} is omitted, independent and identical
N(0, 5^2) prior distributions are placed on each object quality parameter.

If \code{lambda.initial} is omitted, it is set to a vector of zeroes.

If \code{lambda.var} is omitted, it is set to N(0, 5^2).

#' If \code{kappa.var} is omitted, it is set to N(0, 5^2), if \code{kappa.initial} is omitted
it is set to 0.5.
}
\keyword{internal}
